import dbQuery from "../db/dev/dbQuery";

/**
 * @param {object} req
 * @param {object} res
 * @param {array} reservation obj
 * */

const createReservation = async (req, res) => {
  console.log(req);
};

export {
  createReservation,
};
